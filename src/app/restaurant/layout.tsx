export const metadata = {
  title: 'Restaurant - FoodFly',
  description: 'Browse restaurant menu and place orders',
}

export default function RestaurantLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}
