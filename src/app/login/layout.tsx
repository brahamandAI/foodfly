export const metadata = {
  title: 'Sign In - FoodFly',
  description: 'Sign in to your FoodFly account',
}

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}
