# Safe deployment: do not run lifecycle scripts during install
# (postinstall, preinstall, prepare, etc. are a common malware vector)
ignore-scripts=true

# Optional: audit on install (npm 6.14+)
# audit=true

# Lockfile: use exact versions in production
save-exact=false
